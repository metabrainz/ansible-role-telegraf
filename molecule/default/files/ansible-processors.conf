# Ansible managed: Do NOT edit this file manually!

[[processors.regex]]
  namepass = ["net"]
  [[processors.regex.tags]]
    key = "interface"
    pattern = "^eno1$"
    replacement = "eth0"

  [[processors.regex.tags]]
    key = "interface"
    pattern = "^ens1$"
    replacement = "eth1"

